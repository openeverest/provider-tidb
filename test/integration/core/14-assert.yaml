# Validate rejects shrinking a data volume, so the Instance fails instead of
# rolling the change out.
apiVersion: core.openeverest.io/v1alpha1
kind: Instance
metadata:
  name: test-instance
status:
  phase: Failed
